# CV Scoring App (Streamlit + OpenAI)

Streamlit-приложение для оценки соответствия резюме кандидата описанию вакансии с использованием OpenAI.

Проект содержит **два независимых варианта приложения**, реализующих одну задачу разной сложности.

---

## 📌 Назначение проекта

- автоматизировать первичную оценку резюме под конкретную вакансию
- показать, как можно использовать LLM в роли рекрутёра
- продемонстрировать работу как с ручным вводом данных, так и с автопарсингом

---

## 📂 Структура проекта



cv-scoring-streamlit/
├── basic_app.py # Упрощённый вариант (ручной ввод)
├── hh_parsing_app.py # Расширенный вариант (парсинг + ручной ввод)
├── parse_hh.py # Логика парсинга вакансий и резюме (HeadHunter)
├── prompts/
│ └── system_prompt.txt # Системный промпт для LLM
├── .streamlit/
│ └── secrets.toml.example # Пример secrets (без ключа)
├── requirements.txt
├── README.md
├── .gitignore
└── LICENSE


---

## 🧩 Варианты приложения

### 🔹 1. `basic_app.py` — упрощённый вариант

**Назначение**
- ручной ввод текста вакансии и резюме
- системный промпт задан непосредственно в коде

**Используется, когда**
- не нужен автопарсинг
- требуется простой и наглядный пример
- важна минимальная сложность кода

**Запуск**
```bash
streamlit run basic_app.py

🔹 2. hh_parsing_app.py — расширенный вариант

Назначение

автозагрузка вакансии и резюме по ссылке (HeadHunter)

возможность отредактировать текст вручную

системный промпт хранится отдельно (prompts/system_prompt.txt)

Используется, когда

нужно автоматизировать ввод данных

важна гибкость и расширяемость

требуется более реалистичный сценарий использования

Запуск

streamlit run hh_parsing_app.py

⚙️ Установка и запуск
1. Клонировать репозиторий
git clone https://github.com/zerosergey2024/cv-scoring-streamlit.git
cd cv-scoring-streamlit

2. Создать виртуальное окружение
python -m venv venv


Windows (PowerShell):

.\venv\Scripts\Activate.ps1

3. Установить зависимости
pip install -r requirements.txt

🔐 Настройка OpenAI API Key

Создайте файл .streamlit/secrets.toml на основе примера:

cp .streamlit/secrets.toml.example .streamlit/secrets.toml


И укажите ключ:

OPENAI_API_KEY = "your-openai-api-key-here"


Файл .streamlit/secrets.toml:

используется Streamlit

не коммитится в репозиторий

🧠 Логика оценки

Модель получает:

описание вакансии

текст резюме

системный промпт (роль рекрутёра)

На выходе формируется структурированный ответ:

оценка соответствия

сильные стороны

пробелы и риски

рекомендации

Формат ответа — Markdown.

⚠️ Ограничения

парсинг HeadHunter может быть ограничен:

HTTP 403 / 429

изменением HTML-структуры сайта

при недоступности парсинга всегда доступен ручной ввод

📦 Используемые технологии

Python

Streamlit

OpenAI API

Requests

BeautifulSoup

📄 Лицензия

MIT License
