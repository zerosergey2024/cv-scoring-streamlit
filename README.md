# CV Scoring App (Streamlit + OpenAI)

Streamlit-приложение для оценки соответствия резюме кандидата описанию вакансии с использованием OpenAI.

Проект содержит **два режима работы**:
1. **Упрощённый** — ручной ввод вакансии и резюме.
2. **Расширенный** — автоматический парсинг вакансии и резюме (HeadHunter) с возможностью ручного редактирования.

---

## 📂 Структура проекта

cv-scoring-streamlit/
├── apps/
│ ├── basic_app.py # Упрощённый режим (ручной ввод)
│ └── hh_parsing_app.py # Расширенный режим (парсинг + ручной ввод)
├── parse_hh.py # Парсер вакансий и резюме (HeadHunter)
├── prompts/
│ └── system_prompt.txt # Системный промпт для LLM
├── .streamlit/
│ └── secrets.toml.example # Пример secrets (без ключа)
├── requirements.txt
├── README.md
├── .gitignore
└── LICENSE


---

## ⚙️ Установка

### 1. Клонировать репозиторий
```bash
git clone https://github.com/zerosergey2024/cv-scoring-streamlit.git
cd cv-scoring-streamlit

2. Создать виртуальное окружение

python -m venv venv

Windows (PowerShell):

.\venv\Scripts\Activate.ps1

3. Установить зависимости

pip install -r requirements.txt

🔐 Настройка OpenAI API Key

Создайте файл .streamlit/secrets.toml на основе примера:

cp .streamlit/secrets.toml.example .streamlit/secrets.toml

Заполните ключ:

OPENAI_API_KEY = "your-openai-api-key-here"

❗ Файл .streamlit/secrets.toml не коммитится и хранится только локально.
▶️ Запуск приложения
🔹 Режим 1: Упрощённый (ручной ввод)

    Системный промпт задан в коде

    Вакансия и резюме вводятся вручную

streamlit run apps/basic_app.py

🔹 Режим 2: Расширенный (парсинг + ручной ввод)

    Поддержка автозагрузки вакансии и резюме по ссылке (HeadHunter)

    Возможность отредактировать текст вручную

    Системный промпт загружается из файла prompts/system_prompt.txt

streamlit run apps/hh_parsing_app.py

🧠 Логика оценки

Модель получает:

    описание вакансии

    текст резюме

    системный промпт (роль рекрутёра)

И возвращает:

    оценку соответствия

    сильные стороны

    риски / пробелы

    рекомендации

Формат ответа — структурированный Markdown.
⚠️ Ограничения

    Парсинг HeadHunter может быть ограничен:

        HTTP 403 / 429

        изменением HTML-структуры

    В этих случаях рекомендуется использовать ручной ввод текста

📦 Используемые технологии

    Python

    Streamlit

    OpenAI API

    BeautifulSoup (HTML parsing)

    Requests

📄 Лицензия

MIT License
